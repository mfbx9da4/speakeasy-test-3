/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

/**
 * A namespace contains many revisions.
 */
export type Namespace = {
    /**
     * {organization_slug}/{workspace_slug}/{namespace_name}
     */
    id: string;
    /**
     * A human-readable name for the namespace.
     */
    name: string;
    createdAt: Date;
    updatedAt: Date;
};

/** @internal */
export namespace Namespace$ {
    export const inboundSchema: z.ZodType<Namespace, z.ZodTypeDef, unknown> = z
        .object({
            id: z.string(),
            name: z.string(),
            created_at: z
                .string()
                .datetime({ offset: true })
                .transform((v) => new Date(v)),
            updated_at: z
                .string()
                .datetime({ offset: true })
                .transform((v) => new Date(v)),
        })
        .transform((v) => {
            return remap$(v, {
                created_at: "createdAt",
                updated_at: "updatedAt",
            });
        });

    export type Outbound = {
        id: string;
        name: string;
        created_at: string;
        updated_at: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, Namespace> = z
        .object({
            id: z.string(),
            name: z.string(),
            createdAt: z.date().transform((v) => v.toISOString()),
            updatedAt: z.date().transform((v) => v.toISOString()),
        })
        .transform((v) => {
            return remap$(v, {
                createdAt: "created_at",
                updatedAt: "updated_at",
            });
        });
}
