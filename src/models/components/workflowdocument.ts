/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as z from "zod";

export type Auth = {
    header: string;
    secret: string;
};

/**
 * A document referenced by a workflow
 */
export type WorkflowDocument = {
    location: string;
    auth?: Auth | undefined;
};

/** @internal */
export namespace Auth$ {
    export const inboundSchema: z.ZodType<Auth, z.ZodTypeDef, unknown> = z.object({
        header: z.string(),
        secret: z.string(),
    });

    export type Outbound = {
        header: string;
        secret: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, Auth> = z.object({
        header: z.string(),
        secret: z.string(),
    });
}

/** @internal */
export namespace WorkflowDocument$ {
    export const inboundSchema: z.ZodType<WorkflowDocument, z.ZodTypeDef, unknown> = z.object({
        location: z.string(),
        auth: z.lazy(() => Auth$.inboundSchema).optional(),
    });

    export type Outbound = {
        location: string;
        auth?: Auth$.Outbound | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, WorkflowDocument> = z.object({
        location: z.string(),
        auth: z.lazy(() => Auth$.outboundSchema).optional(),
    });
}
