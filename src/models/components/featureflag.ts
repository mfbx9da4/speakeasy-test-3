/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

/**
 * A feature flag is a key-value pair that can be used to enable or disable features.
 */
export type FeatureFlag = {
    featureFlag: string;
    trialEndsAt?: Date | null | undefined;
};

/** @internal */
export namespace FeatureFlag$ {
    export const inboundSchema: z.ZodType<FeatureFlag, z.ZodTypeDef, unknown> = z
        .object({
            feature_flag: z.string(),
            trial_ends_at: z
                .nullable(
                    z
                        .string()
                        .datetime({ offset: true })
                        .transform((v) => new Date(v))
                )
                .optional(),
        })
        .transform((v) => {
            return remap$(v, {
                feature_flag: "featureFlag",
                trial_ends_at: "trialEndsAt",
            });
        });

    export type Outbound = {
        feature_flag: string;
        trial_ends_at?: string | null | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, FeatureFlag> = z
        .object({
            featureFlag: z.string(),
            trialEndsAt: z.nullable(z.date().transform((v) => v.toISOString())).optional(),
        })
        .transform((v) => {
            return remap$(v, {
                featureFlag: "feature_flag",
                trialEndsAt: "trial_ends_at",
            });
        });
}
