/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

/**
 * Request body of tags to add to a revision
 */
export type AddTags = {
    /**
     * revision digest to add tags too sha256:...
     */
    revisionDigest: string;
    /**
     * string tags to add to the revision
     */
    tags: Array<string>;
};

/** @internal */
export namespace AddTags$ {
    export const inboundSchema: z.ZodType<AddTags, z.ZodTypeDef, unknown> = z
        .object({
            revision_digest: z.string(),
            tags: z.array(z.string()),
        })
        .transform((v) => {
            return remap$(v, {
                revision_digest: "revisionDigest",
            });
        });

    export type Outbound = {
        revision_digest: string;
        tags: Array<string>;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, AddTags> = z
        .object({
            revisionDigest: z.string(),
            tags: z.array(z.string()),
        })
        .transform((v) => {
            return remap$(v, {
                revisionDigest: "revision_digest",
            });
        });
}
