/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

export type GenerateOpenApiSpecDiff = {
    currentSchema: string;
    newSchema: string;
};

/** @internal */
export namespace GenerateOpenApiSpecDiff$ {
    export const inboundSchema: z.ZodType<GenerateOpenApiSpecDiff, z.ZodTypeDef, unknown> = z
        .object({
            current_schema: z.string(),
            new_schema: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                current_schema: "currentSchema",
                new_schema: "newSchema",
            });
        });

    export type Outbound = {
        current_schema: string;
        new_schema: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, GenerateOpenApiSpecDiff> = z
        .object({
            currentSchema: z.string(),
            newSchema: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                currentSchema: "current_schema",
                newSchema: "new_schema",
            });
        });
}
