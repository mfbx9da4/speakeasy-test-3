/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

/**
 * A PreflightToken is a token that allows access to the OCI distribution endpoints.
 */
export type PreflightToken = {
    authToken?: string | undefined;
};

/** @internal */
export namespace PreflightToken$ {
    export const inboundSchema: z.ZodType<PreflightToken, z.ZodTypeDef, unknown> = z
        .object({
            auth_token: z.string().optional(),
        })
        .transform((v) => {
            return remap$(v, {
                auth_token: "authToken",
            });
        });

    export type Outbound = {
        auth_token?: string | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, PreflightToken> = z
        .object({
            authToken: z.string().optional(),
        })
        .transform((v) => {
            return remap$(v, {
                authToken: "auth_token",
            });
        });
}
