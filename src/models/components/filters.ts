/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { Filter, Filter$ } from "./filter.js";
import * as z from "zod";

/**
 * Filters are used to query requests.
 */
export type Filters = {
    /**
     * A list of filters to apply to the query.
     */
    filters: Array<Filter>;
    /**
     * The maximum number of results to return.
     */
    limit: number;
    /**
     * The offset to start the query from.
     */
    offset: number;
    /**
     * The operator to use when combining filters.
     */
    operator: string;
};

/** @internal */
export namespace Filters$ {
    export const inboundSchema: z.ZodType<Filters, z.ZodTypeDef, unknown> = z.object({
        filters: z.array(Filter$.inboundSchema),
        limit: z.number().int(),
        offset: z.number().int(),
        operator: z.string(),
    });

    export type Outbound = {
        filters: Array<Filter$.Outbound>;
        limit: number;
        offset: number;
        operator: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, Filters> = z.object({
        filters: z.array(Filter$.outboundSchema),
        limit: z.number().int(),
        offset: z.number().int(),
        operator: z.string(),
    });
}
