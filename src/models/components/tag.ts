/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

export type Tag = {
    /**
     * Format {namespace_id}/{tag}
     */
    id: string;
    /**
     * Human readable tag name
     */
    name: string;
    namespaceName: string;
    revisionDigest: string;
};

/** @internal */
export namespace Tag$ {
    export const inboundSchema: z.ZodType<Tag, z.ZodTypeDef, unknown> = z
        .object({
            id: z.string(),
            name: z.string(),
            namespace_name: z.string(),
            revision_digest: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                namespace_name: "namespaceName",
                revision_digest: "revisionDigest",
            });
        });

    export type Outbound = {
        id: string;
        name: string;
        namespace_name: string;
        revision_digest: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, Tag> = z
        .object({
            id: z.string(),
            name: z.string(),
            namespaceName: z.string(),
            revisionDigest: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                namespaceName: "namespace_name",
                revisionDigest: "revision_digest",
            });
        });
}
