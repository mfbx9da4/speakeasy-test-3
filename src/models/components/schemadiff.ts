/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

export type ValueChange = {
    /**
     * Represents the previous value of the element.
     */
    from: string;
    /**
     * Represents the current value of the element.
     */
    to: string;
};

/**
 * A SchemaDiff represents a diff of two Schemas.
 */
export type SchemaDiff = {
    /**
     * Holds every addition change in the diff.
     */
    additions: Array<string>;
    /**
     * Holds every deletion change in the diff.
     */
    deletions: Array<string>;
    /**
     * Holds every modification change in the diff.
     */
    modifications: { [k: string]: ValueChange };
};

/** @internal */
export namespace ValueChange$ {
    export const inboundSchema: z.ZodType<ValueChange, z.ZodTypeDef, unknown> = z
        .object({
            From: z.string(),
            To: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                From: "from",
                To: "to",
            });
        });

    export type Outbound = {
        From: string;
        To: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, ValueChange> = z
        .object({
            from: z.string(),
            to: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                from: "From",
                to: "To",
            });
        });
}

/** @internal */
export namespace SchemaDiff$ {
    export const inboundSchema: z.ZodType<SchemaDiff, z.ZodTypeDef, unknown> = z.object({
        additions: z.array(z.string()),
        deletions: z.array(z.string()),
        modifications: z.record(z.lazy(() => ValueChange$.inboundSchema)),
    });

    export type Outbound = {
        additions: Array<string>;
        deletions: Array<string>;
        modifications: { [k: string]: ValueChange$.Outbound };
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, SchemaDiff> = z.object({
        additions: z.array(z.string()),
        deletions: z.array(z.string()),
        modifications: z.record(z.lazy(() => ValueChange$.outboundSchema)),
    });
}
