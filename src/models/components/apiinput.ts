/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

/**
 * An Api is representation of a API (a collection of API Endpoints) within the Speakeasy Platform.
 */
export type ApiInput = {
    /**
     * The ID of this Api. This is a human-readable name (subject to change).
     */
    apiId: string;
    /**
     * A detailed description of the Api.
     */
    description: string;
    /**
     * A set of values associated with a meta_data key. This field is only set on get requests.
     */
    metaData?: { [k: string]: Array<string> } | undefined;
    /**
     * The version ID of this Api. This is semantic version identifier.
     */
    versionId: string;
};

/** @internal */
export namespace ApiInput$ {
    export const inboundSchema: z.ZodType<ApiInput, z.ZodTypeDef, unknown> = z
        .object({
            api_id: z.string(),
            description: z.string(),
            meta_data: z.record(z.array(z.string())).optional(),
            version_id: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                api_id: "apiId",
                meta_data: "metaData",
                version_id: "versionId",
            });
        });

    export type Outbound = {
        api_id: string;
        description: string;
        meta_data?: { [k: string]: Array<string> } | undefined;
        version_id: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, ApiInput> = z
        .object({
            apiId: z.string(),
            description: z.string(),
            metaData: z.record(z.array(z.string())).optional(),
            versionId: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                apiId: "api_id",
                metaData: "meta_data",
                versionId: "version_id",
            });
        });
}
