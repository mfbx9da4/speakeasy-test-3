/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

export type OASOperation = {
    method: string;
    path: string;
    operationId: string;
    description: string;
    tags: Array<string>;
};

/** @internal */
export namespace OASOperation$ {
    export const inboundSchema: z.ZodType<OASOperation, z.ZodTypeDef, unknown> = z
        .object({
            method: z.string(),
            path: z.string(),
            operation_id: z.string(),
            description: z.string(),
            tags: z.array(z.string()),
        })
        .transform((v) => {
            return remap$(v, {
                operation_id: "operationId",
            });
        });

    export type Outbound = {
        method: string;
        path: string;
        operation_id: string;
        description: string;
        tags: Array<string>;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, OASOperation> = z
        .object({
            method: z.string(),
            path: z.string(),
            operationId: z.string(),
            description: z.string(),
            tags: z.array(z.string()),
        })
        .transform((v) => {
            return remap$(v, {
                operationId: "operation_id",
            });
        });
}
