/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

/**
 * An EmbedAccessTokenResponse contains a token that can be used to embed a Speakeasy dashboard.
 */
export type EmbedAccessTokenResponse = {
    accessToken: string;
};

/** @internal */
export namespace EmbedAccessTokenResponse$ {
    export const inboundSchema: z.ZodType<EmbedAccessTokenResponse, z.ZodTypeDef, unknown> = z
        .object({
            access_token: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                access_token: "accessToken",
            });
        });

    export type Outbound = {
        access_token: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, EmbedAccessTokenResponse> = z
        .object({
            accessToken: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                accessToken: "access_token",
            });
        });
}
