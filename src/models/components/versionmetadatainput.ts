/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

/**
 * A set of keys and associated values, attached to a particular version of an Api.
 */
export type VersionMetadataInput = {
    /**
     * The key for this metadata.
     */
    metaKey: string;
    /**
     * One of the values for this metadata.
     */
    metaValue: string;
};

/** @internal */
export namespace VersionMetadataInput$ {
    export const inboundSchema: z.ZodType<VersionMetadataInput, z.ZodTypeDef, unknown> = z
        .object({
            meta_key: z.string(),
            meta_value: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                meta_key: "metaKey",
                meta_value: "metaValue",
            });
        });

    export type Outbound = {
        meta_key: string;
        meta_value: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, VersionMetadataInput> = z
        .object({
            metaKey: z.string(),
            metaValue: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                metaKey: "meta_key",
                metaValue: "meta_value",
            });
        });
}
